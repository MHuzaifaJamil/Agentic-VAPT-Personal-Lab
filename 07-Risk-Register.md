# Risk Register — Autonomous Agentic VAPT System

Likelihood/Impact: **L/M/H**. Severity is a qualitative combination, not a formula.
"Status" reflects what this planning phase has actually done about each risk — a
requirement reference means it's mitigated *on paper*; "Residual/Accepted" means the
risk was explicitly weighed and kept as a known trade-off, not overlooked.

| ID | Risk | Likelihood | Impact | Severity | Mitigation | Status |
|---|---|---|---|---|---|---|
| RISK-MEMEXHAUST | Combined model + tool subprocess memory exceeds available RAM mid-engagement, crashing the process. | M | H | High | NFR-RES-02 (1.5 GB margin), OPS-MONITOR-01/02 (pause on breach), FR-GATE-10/IR-ENGINE-06 (settle-poll gate closes the model-swap-specific exhaustion window — see `RISK-MEMSETTLERACE`) | Mitigated |
| RISK-OOMKILL | Kernel OOM killer reaps a frozen (`SIGSTOP`) application during the Phase 1 memory-pressure spike, causing real data loss despite "hibernation." | M | H | High | FR-ENV-11 (`oom_score_adj = -900` deprioritization, confirmed value), FR-ENV-12 (post-hoc verification + degraded-state reporting), FR-ENV-13 (the privileged helper that actually makes FR-ENV-07's page-out call possible — see `RISK-MADVISEPERM`) | Mitigated (best-effort — see NFR-REL-06 wording) |
| RISK-THERMAL | Sustained AVX2/AVX-VNNI inference load throttles the mobile CPU over a multi-hour session, invalidating throughput assumptions. | M | M | Medium | OPS-MONITOR-03 — **contingent on an unverified feasibility check** (is thermal telemetry even exposed on this hardware/kernel?) | Partially mitigated — feasibility unconfirmed |
| RISK-GPUOFFLOAD | Intel Arc iGPU SYCL/Level-Zero backend is immature/unstable for this kernel+driver combination, silently degrading to CPU-only. | M | M | Medium | FR-GATE-04 (fall back, log as degraded, don't fail engagement) | Mitigated (graceful degradation, not prevention) |
| RISK-PROMPTINJECT | A scanned target's own response content manipulates a council model (e.g., forged "mark CONFIRMED" or scope-expansion text). | M | H | High | FR-TOOL-12/13, IR-SANITIZE-02/03, SEC-PROMPT-01..04 (provenance tagging + instruction hierarchy + heuristic detection) | Mitigated (MUST, per explicit decision) |
| RISK-UNCENSOREDGATE | Council Gate 1's base-plan model (Hermes-3) was selected specifically for compliant/uncensored steerability, the opposite property wanted from a refusal-oriented safety gate. | L (down from M) | H | Medium (down from High) | Model swapped to `Llama-3.1-8B-Instruct` for conservative refusal behavior, plus a new non-bypassable deterministic pre-check tier (`FR-COUNCIL-03a`) that doesn't depend on any LLM's judgment at all; further backed by `RISK-PROMPTINJECT`'s controls | **Mitigated** — model choice changed (C-03 resolution); residual risk is that the new model's refusal behavior is reasoned, not yet empirically tested (`10`, Open Item C) |
| RISK-CVSSACCURACY | An 8B distilled LLM's autonomously-assigned CVSS score is inaccurate, undermining report credibility. | H (if unmitigated) | M | Medium-High | FR-COUNCIL-16a (deterministic calculator computes the final score from LLM-proposed metrics, not the LLM directly) | Mitigated |
| RISK-TIER2RESIDUAL | Path-restricted allowlist (`/usr/bin`,`/usr/sbin`,`/opt`) still permits essentially the full Kali offensive toolset to run autonomously and unattended for up to 12 hours. | M (down from H) | H | Medium-High | Behavioral denylist (FR-TOOL-06 a-e); the three highest-risk categories (brute-force, active-exploitation, lateral-movement — FR-TOOL-06a) now additionally require an explicit per-category opt-in flag. Tools outside those three lists still depend on Gate 1/Gate 2 correctness (SEC-CONTAIN-02). | **Partially mitigated** — narrowed by the opt-in flag mechanism (C-14 resolution); residual risk remains for non-listed tools and for any category once its flag is enabled |
| RISK-UNBOUNDEDAUTONOMY | No authorization/RoE gate exists (by decision) **and** no operator pause exists in the Phase 4.2 loop (by decision) — the system can autonomously exploit real targets for up to 12 hours with no built-in authorization check and no forced human checkpoint. | M (depends entirely on operator's own discipline outside the tool) | H | **High** | None at the system level — flagged explicitly to the operator during planning (see `10-Decision-Log...md`) and left as-is per explicit operator decision | **Residual/Accepted — flagged, not reversed** |
| RISK-ENGINEAMBIGUITY | Base plan treated `llama.cpp`/`ollama` as interchangeable; the described `keep_alive` swap behavior is Ollama-specific and wouldn't work as-is on raw `llama.cpp`. | H (if unresolved) | M | Medium | IR-ENGINE-01..05 (Local Engine Client abstraction, `llama.cpp` as primary with explicit process lifecycle management) | Mitigated |
| RISK-SWAPWEAR | Daily hibernation cycles (paging 3.5-4.5 GB to NVMe swap) accelerate SSD wear over the device's operational life. | L-M (depends on usage frequency) | M | Medium | NFR-REL-05 (log cumulative swap write volume for visibility) — no hard limit imposed | Partially mitigated (visibility only) |
| RISK-DBLOCKCONTENTION | SQLite's default journal mode blocks concurrent reads (e.g., a `status` query) during a write. | M | L | Low | DR-CONCURRENCY-01 (WAL mode) | Mitigated |
| RISK-LOGVOLUME | A 12-hour, multi-target, 30-tasks-per-target session generates enough raw tool output + logs to threaten the disk-quota thresholds. | M | M | Medium | NFR-RES-04 thresholds apply to logs too (OPS-LOG-02); DR-RETENTION-01 | Mitigated (detection/block, not prevention of growth) |
| RISK-FALSEPOSITIVE | Gate 3 (Mistral-7B) misjudges a `CONFIRMED`/`DISMISSED` call; base plan's "completely mitigates hallucination" framing overstates reliability (finding C-11). | M | M | Medium | Gate 3's structured false-positive-pattern checklist (FR-COUNCIL-14); no further mitigation added — reporting/acceptance criteria (`09`) should not assume zero false positives | Partially mitigated — inherent LLM-judgment limitation, not fully closeable |
| RISK-TOOLDECAY | `nuclei` templates, CVE feeds, and wordlists go stale over time with no update mechanism defined. | H (over time) | M | Medium | None — explicitly deferred (OPS-MAINT-01) | **Deferred, not mitigated** — out of scope for this planning phase |
| RISK-CORPUSDRIFT | This is a fresh independent-researcher report identity with no existing verified report to serve as the "canonical reference template" (§13 of `12-Report-Formatting-Rules.md`). | M (until first report exists) | L | Low | Written rules (§1-§12) stand in until a first verified report exists | Mitigated (procedural) |
| RISK-CROSSMACHINE | The report-formatting rules and this entire requirements set were authored on a different machine (this planning session's host) than the one the system will actually run on (per base doc §1, a separate PC with `/home/mhj/...` paths). Files created here don't automatically exist there. | H (certain, until transferred) | M | Medium | None yet — flagged as a dependency in `08-Assumptions-Constraints-Dependencies.md` | **Open — operator action required** (transfer this doc set, and ideally the whole `claude-bug-bounty` toolkit, to the target machine) |
| RISK-MADVISEPERM | `process_madvise(MADV_PAGEOUT)` requires `CAP_SYS_PTRACE`/`CAP_SYS_NICE`, which the least-privileged agent process (NFR-SEC-03) does not hold by design — the call would fail with `EPERM` if not addressed. | H (certain, if unaddressed) | H | High | FR-ENV-13 (narrow `setcap`/`sudoers`-scoped helper), cgroup v2 fallback if unavailable | Mitigated (finding C-15) |
| RISK-STALESOCKETS | Freezing desktop apps for 10-12 hours lapses their network/IPC sessions; on resume, apps may show reconnect/re-auth prompts or, in rare cases, discard state if the app itself forces a reload on stale-session detection. | H (network lapse is near-certain over that duration) | L-M | Medium | FR-ENV-14 (SLA reframed to process-memory guarantee only, reconnect logged as expected/informational) | Mitigated at the SLA/documentation level (finding C-16) — the underlying network lapse itself is not preventable |
| RISK-NOISYYIELD | A naive "yield" definition (non-empty output / exit 0) lets noisy tools (`ffuf`, `gobuster`) reset the zero-yield circuit breaker indefinitely, burning the entire 30-task-per-target budget on one unproductive target. | H (if unaddressed, common with wildcard/soft-404 responses) | M | Medium-High | FR-COUNCIL-11a + `discovered_entities` state-delta ledger (DR-SCHEMA-12) | Mitigated (finding C-17) |
| RISK-MEMSETTLERACE | Spawning the next model process immediately after the outbound one exits risks a transient window where the kernel hasn't finished reclaiming pages, potentially tripping the already-thin 1.5 GB safety margin. | M | M | Medium | FR-GATE-10/IR-ENGINE-06 (`MemAvailable` poll gate before spawn, 5s bound, degrades gracefully) | Mitigated (finding C-18) |

## Risks Explicitly Not Mitigated By Design (documented trade-offs)

Three risks above — **RISK-UNCENSOREDGATE**, **RISK-TIER2RESIDUAL**, and
**RISK-UNBOUNDEDAUTONOMY** — remain open by deliberate operator decision rather than
oversight. They are the highest-severity items in this register and the ones most
worth revisiting if the intended use case ever shifts from a personal/lab environment
toward engagements involving other parties' infrastructure or contractual liability.

# Decision Log & Open Questions — Autonomous Agentic VAPT System

Every substantive decision in `01`-`09`/`11`/`12` traces back to an explicit answer
given during this planning session — none were assumed. This document is the
chronological record of *why* the requirements read the way they do, so a future
reader isn't left guessing why (for example) there's no authorization gate, or why
Council Gate 1's semantic tier uses `Hermes-3-Llama-3.1-8B` (see decision #55, which
reversed decision #34's earlier swap away from it).

---

## Decisions On Record

| # | Decision | Rationale / Trigger |
|---|---|---|
| 1 | `/home/mhj/...` paths kept verbatim, not updated to this session's machine | Base doc describes a different physical PC than this planning session ran on |
| 2 | Operator control surface (start/pause/resume/abort/status/export) kept as a real requirement | "There should be both Automation and Control Simultaneously" |
| 3 | Authorization / Rules-of-Engagement verification is explicitly **out of scope** for the system | Operator decision — obtaining authorization is the operator's own responsibility outside the tool (see `AC-ASSUME-03` for the load-bearing assumption this creates) |
| 4 | Numeric/design parameters: ask each one individually rather than propose defaults | Explicit process instruction, applied for the rest of the session |
| 5 | RAM safety margin = 1.5 GB | Direct answer |
| 6 | Disk thresholds = 85% warn / 95% block | Direct answer |
| 7 | Model-swap time budget = 60s | Direct answer |
| 8 | Kill-switch full-stop budget = 20s | Direct answer |
| 9 | E-core allocation for tool subprocesses = 4 of 8 threads | Direct answer |
| 10 | Report pipeline: Markdown first → held in `pending-approval/` → operator approves → HTML + PDF via `pandoc`+`wkhtmltopdf`/`weasyprint` | Direct answer |
| 11 | Report formatting rules cloned from `claude-bug-bounty/CLAUDE.md`, identity replaced (Muhammad Huzaifa Jamil / protonmail), single-assessor footer, original file untouched, saved as `12-Report-Formatting-Rules.md` | Direct answers (two-question round) |
| 12 | Multi-target support: one engagement may scope multiple hosts/domains | Direct answer — drove the `engagements`/`targets` schema split in `03` |
| 13 | Phase 4.2 loop bound: 30-task-per-target cap + 3-zero-yield circuit breaker + 12-hour global budget, auto-pivot/auto-report, **no operator pause** | Direct, detailed answer |
| 14 | GitHub repo `MHuzaifaJamil/Agentic-VAPT-Personal-Lab`, private, `gh` CLI (self-installed by operator), local git identity `Muhammad Huzaifa Jamil <muhammad.huzaifa.jamil@gmail.com>` | Direct answers across a multi-step setup exchange |
| 15 | Pushback logged: combining no-RoE-gate (#3) with no-pause 12h autonomy (#13) creates `RISK-UNBOUNDEDAUTONOMY` | Raised proactively per "counter unrealistic responses" instruction; **not reversed** by operator — kept as documented residual risk |
| 16 | Prompt-injection defense (critical-analysis C-04) = **MUST** requirement | Direct answer |
| 17 | Hibernation OOM protection (C-01) = mandate `oom_score_adj` deprioritization | Direct answer |
| 18 | CVSS scoring (C-07) = deterministic calculator computes the score; LLM only proposes metrics | Direct answer |
| 19 | Operator control interface = **CLI only**, no GUI/dashboard | Direct answer |
| 20 | Tier 2 bridge safety (C-12): path-restricted allowlist (`/usr/bin`,`/usr/sbin`,`/opt`) + behavioral denylist, no per-binary approval by default | Direct answer (rejected the original multiple-choice framing; operator supplied their own more specific mechanism) |
| 21 | Inference engine (C-13): `llama.cpp --server` primary, explicit process-lifecycle model swap, abstracted behind a Local Engine Client so `ollama` can substitute later | Direct answer (operator supplied their own mechanism, not one of the offered options) |
| 22 | Evidence redaction reconciliation: draft redacted by default, reversibly unredacted only on operator's `approve-report` action | Direct answer, resolving a real contradiction between `FR-COUNCIL-18` and the cloned report rules' "never redact" clause |
| 23 | C-02 (memory-reclamation figures): add an explicit "illustrative, not guaranteed" framing note to `02`, on top of the already-existing `FR-ENV-08` live re-measurement | Direct answer |
| 24 | C-06 (`i915`/`xe` claim): documentation caveat only, no new pre-flight check | Direct answer |
| 25 | C-11 (hallucination overclaim): downgraded language + existing Gate 3 checklist judged sufficient, no new control | Direct answer |
| 26 | C-14 (Tier 2 residual risk): three curated high-risk categories, each gated by its own pre-engagement opt-in flag (`--allow-brute-force`, `--allow-active-exploitation`, `--allow-lateral-movement`), settable at `start` and updatable via `resume`; unpermitted tasks are refused and the loop auto-continues, no mid-run halt | Multi-round direct answers (category count, full binary lists, flag names, mid-engagement mutability, default-for-unlisted-tools) |
| 27 | CVSS version standardized on **3.1** | Direct answer |
| 28 | GPU-offload benchmark pass bar (`FR-PRE-08`): relative — GPU offload must beat CPU-only in the same benchmark, no fixed tok/s number | Direct answer |
| 29 | Thermal-throttle trigger (`OPS-MONITOR-03`): use the kernel's own reported throttle/PROCHOT signal, not a guessed °C threshold | Direct answer |
| 30 | Gate 2 correction attempts before `BLOCKED` (`FR-COUNCIL-09`) = 3 | Direct answer (changed from an earlier un-confirmed default of 2) |
| 31 | Hibernation confirmation (`FR-ENV-06`): no runtime interactive prompt — invoking `start` is itself the consent; resolves a contradiction with the fully-unattended design | Direct answer |
| 32 | `state.db` backup (`DR-BACKUP-01`): **MUST**, not best-effort — Phase 5 isn't complete without it | Direct answer |
| 33 | Generic "autonomy levels" (paranoid/normal/yolo) concept: **removed** | Direct answer — judged redundant once the three opt-in flags (#26) and fixed loop-bound thresholds (#13) existed; all references across `01`/`02`/`03`/`05` updated accordingly |
| 34 | Council Gate 1 model swap (C-03): replace `Hermes-3-Llama-3.1-8B` with `Llama-3.1-8B-Instruct` for the semantic layer, **plus** add a new non-LLM deterministic scope-checker as a mandatory first tier (`FR-COUNCIL-03a`); `Mistral-7B` stays dedicated to Gate 3 only | Direct answer — operator supplied the specific mechanism (not one of the offered options) |
| 35 | Exec-loop model-swap granularity (C-09): **zero swapping** inside the active loop — `Qwen2.5-Coder-7B` stays resident for the whole per-target loop; Gate 2 becomes a deterministic Python validator; `Qwen2.5-Coder-3B` demoted to an offline, between-phase role only | Direct answer — operator supplied the specific mechanism (not one of the offered options) |
| 36 | Tiered subprocess timeouts (C-08): Quick Probes 180s / Targeted Scans 900s / Deep-Full-Range 1800s, plus mandatory non-blocking output streaming to detect stalls before the hard timeout | Direct answer, with an explicit tool-to-tier mapping |
| 37 | `gobuster`/`feroxbuster`/`testssl` explicitly confirmed into the 900s Targeted Scans tier (this was an inference in `IR-TOOL-03`, now closed) | Direct answer |
| 38 | `NFR-PERF-04` (an un-confirmed `[PROPOSED]` item predating the 12-hour budget) removed as redundant with `NFR-PERF-05` | Direct answer |
| 39 | **Reversal of decision #1-era policy:** `Agentic VAPT Setup (HOME).md` — previously off-limits to editing — is now directly corrected in place (silent replacement style) for 9 items: the Gate 1/Gate 2 model-and-mechanism swap (C-03/C-09), CVSS scoring (C-07), Tier 2 bridge safety + opt-in flags (C-12/C-14), inference engine (C-13), tiered timeouts (C-08), the "zero data loss" claim (C-01), and the unbounded task-queue loop (`FR-COUNCIL-11`). C-02/C-04/C-05/C-06/C-10 were explicitly *not* applied to the source file. Each correction carries an inline note pointing back to `11`. | Direct instruction, explicitly reversing the earlier "do not change the original plan file" instruction |
| 40 | Four new critical-analysis findings added and resolved from an externally-sourced issue list, all analyzed and confirmed as genuine (not just transcribed): **C-15** (`process_madvise` needs privileges the least-privilege design doesn't have → narrow `setcap`/`sudoers`-scoped helper, `FR-ENV-13`, cgroup v2 fallback), **C-16** (long `SIGSTOP` lapses network/IPC sessions → hibernation SLA reframed to process-memory only, `FR-ENV-14`, with an added caveat that some apps may still force-reload on stale-session detection), **C-17** (undefined "zero-yield" let noisy tools defeat the circuit breaker → redefined as a state-delta against a new `discovered_entities` ledger, `FR-COUNCIL-11a`/`DR-SCHEMA-12`), **C-18** (model-swap teardown/respawn race → mandatory `MemAvailable` poll gate before the next model spawns, 5s bound, `FR-GATE-10`/`IR-ENGINE-06`). Also concretized two previously-generic mechanisms with the specific values proposed: the prompt-injection tag is `<tool_output_untrusted>...</tool_output_untrusted>` (was a generic placeholder), and the CVSS 3.1 calculator is specifically the Python `cvss` library. `oom_score_adj` was concretized to `-900`. All of this was also mirrored into the base plan file (decision #39's treatment, extended). The proposed GRUB/kernel-parameter driver-migration remediation (for the `i915`/`xe` mismatch, C-06) was evaluated and kept as a **documented manual recommendation only** — not an automated agent capability — since it is a system-wide, reboot-requiring bootloader change outside this design's risk class. | Operator supplied a detailed externally-sourced issue list and asked for critical analysis + fixes; two scope questions (GRUB automation, base-file mirroring) were asked before implementing |
| 41 | **Implementation architecture bridge (`13-Implementation-Architecture-Bridge.md`).** Before handing the doc set to a build-time coding agent, an honest gap assessment found `01`-`12` were requirements, not a buildable spec: no process/daemon model, no declared language, no file formats, no helper IPC contract, no module layout. Confirmed: (a) process model = per-invocation, SQLite/signal-coordinated, no daemon — `pause` cooperative via `SIGUSR1`+intent flag, `abort` a direct external kill (not cooperative), `resume` spawns a fresh process; (b) language = Python 3.x; (c) scope-rules file = YAML; (d) thresholds = a YAML config file with the confirmed values as defaults (config format extended to YAML by inference from (c), not re-asked); (e) freezer-helper invocation = one-shot subprocess call per operation, not a persistent helper+IPC protocol; (f) CLI framework = Click; (g) a concrete module layout was requested and proposed. Designing the process model surfaced three real schema gaps (no table for suspended-PID tracking, no PID column for `abort` to find a running subprocess, no redaction-mapping table for `FR-COUNCIL-18`) — closed in `03` as `DR-SCHEMA-13`/`14` and new columns. | Two rounds of direct questions (4 questions each) on the foundational forks; the three schema gaps were found and closed while writing, not re-asked (mechanical consequence of the chosen process model, not a new design fork) |
| 42 | **Three more build-level fixes from a second externally-sourced review, all analyzed and confirmed genuine:** C-19 (kill-switch targeted only the recorded PID, not the process group → subprocesses now spawn via `start_new_session=True`, `abort` uses `os.killpg`), C-20 (WAL mode alone doesn't stop "database is locked" between concurrent CLI invocations → `PRAGMA busy_timeout = 5000` on every connection), C-21 (redaction addressing via "offset or regex" could restore the wrong secret on a duplicate token → revised to exact `start_offset`/`end_offset` + `content_hash`, with a hard failure on hash mismatch). **Standing policy established** (applies going forward, not just this batch): `Agentic VAPT Setup (HOME).md` carries **high-level statements only** — no Python-specific flags, SQLite pragmas, or schema-column-level detail — while `01`-`13` carry full precision. Consistency between the two is maintained by paraphrasing each fix at the appropriate altitude for the base file, not by copying the precise mechanism verbatim. All three fixes were mirrored into the base file at that high level. | Operator supplied a second detailed review; explicitly asked for the altitude split as a standing rule rather than a one-off answer |
| 43 | **Structured LLM output reliability (C-22), the sharpest gap found in a self-initiated "if I had to build this" review.** Nothing previously guaranteed Tier 1 tool-call JSON, CVSS metric proposals, or gate decisions were well-formed/schema-conforming. Confirmed: hybrid enforcement — `response_format={"type":"json_object"}` (backend-agnostic, unlike a `llama.cpp`-specific GBNF grammar) for generation-time JSON syntax validity, plus a mandatory deterministic Python schema validator per output type, plus a bounded 2-retry (3 attempts total) repair loop with the validator's error fed back into context. See `04-Interface-and-Integration-Requirements.md` IR-STRUCTURED. | Direct answer, operator's own proposed mechanism, not one of the offered options |
| 44 | **System-wide single-engagement lock (`FR-CTRL-09`).** `start` now refuses if any engagement is already `IN_PROGRESS`/`PAUSED`, enforced both in application code and via a SQLite generated-column + partial-unique-index pattern (`engagement_lock_slot`) so a race between two near-simultaneous `start` calls can't both succeed. | Direct answer |
| 45 | **System prompt templates drafted now, not deferred to build time** (`14-System-Prompt-Templates.md`) — actual prompt text for all 6 prompted roles (Strategist, Gate 1 semantic, Operator, Gate 3, Reporter, offline linter; Gate 1 Tier 0 and Gate 2 are deterministic code with no prompt), each built from a shared provenance/instruction-hierarchy clause and a shared output-format clause. | Direct answer |
| 46 | **Implementation milestone roadmap added** (`15-Implementation-Milestone-Roadmap.md`) — 9 build-ordered, independently-testable milestones, deliberately sequencing hibernation/the privileged helper late (after the core loop is proven) rather than first. | Direct answer |
| 47 | **A second, deliberately careful "check again" pass (not a surface reread) found three more real issues, resolved without further questions since each had only one sound answer, not a genuine fork:** C-23 (redaction timing was never specified; post-processing the Reporter's free-form prose is fragile — resolved by redacting the evidence *before* the Reporter ever sees it, so its own draft never contains a real value to leak), C-24 (the Operator had no visibility into current opt-in-flag state, risking wasted task-budget on predictably-refused proposals — resolved by including flag state in its per-task context), C-25 (the `reports` schema, keyed only by `engagement_id`, couldn't represent `12-Report-Formatting-Rules.md`'s own established format of one report per confirmed finding plus one consolidated informational register — resolved by adding `finding_id`/`document_type` and a partial unique index). | Explicit instruction to "check again and then again, fix all issues first" before any commit — this was a genuine re-read, not a rubber-stamp |
| 48 | **Repo navigation for a build-time coding agent.** Created `CLAUDE.md` at the repo root (auto-read by Claude Code on session start) with a "what am I building, which doc" lookup table and the key gotchas (base file is not authoritative, nothing installed yet, no authorization gate by design, `Actual-Setup/` is a separate reference toolkit). The same table was duplicated into `00-Requirements-Index.md` as a backup in case `CLAUDE.md` is missing or a different tool is used. | Explicit instruction that "enough information" must exist in the files themselves for a developing agent to know what to read and from where |
| 49 | **A third check pass (explicitly requested — "check again and then again") found two more real gaps and one judgment call, applied without re-asking since each had only one reasonable resolution:** (a) the five council model `.gguf` files themselves, and a pinned Python dependency manifest, were never listed as dependencies anywhere in `08` — added as `AC-DEPENDENCY-09`/`10` and `RISK-NOMODELFILES`. (b) The base file's Phase 4.3 wording ("drafts the technical penetration testing report," singular) is now misleading given C-25's per-finding/register distinction — corrected in place at high level, applying the standing altitude policy (decision #42) directly rather than asking again, since that policy's whole purpose was to stop needing to ask each time. C-22/C-23/C-24 were deliberately **not** mirrored to the base file — judged purely additive (no existing claim to correct), consistent with `FR-CTRL` never being mirrored either; this reasoning is stated explicitly in `11` and `00` rather than left implicit, so it can be challenged if wrong. | Explicit instruction to keep checking before any commit |
| 50 | **`Actual-Setup/` reuse formalized** (`16-Actual-Setup-Reuse-and-Integration-Map.md`) — an asset-by-asset analysis, verified by reading actual files rather than the toolkit's own summary. Key findings: nothing in `skills/`/`commands/`/`agents/` is directly executable by this system (Claude-Code-native vs. our fully local architecture); 29 of 44 `tools/*.py` scripts are verified standalone (no cross-module coupling), 15 are coupled; `scope_checker.py` is a near-exact reference implementation for `FR-COUNCIL-03a`; both `mcp/burp-mcp-client` and `mcp/hackerone-mcp` turn out to be plain REST wrappers, not requiring an MCP client on our side at all; `FR-TOOL-03`'s existing `/opt/` allowlist entry already covers deploying adapted scripts, no requirements change needed there (a prior chat-only version of this analysis overstated this as a gap). One methodology-mining item was fully implemented, not just proposed: `skills/triage-validation/SKILL.md` was read in full and its impact/identity/baseline-attack-diff checks folded into `FR-COUNCIL-14a` and the Gate 3 prompt (`14` §4) — three further high-value mining targets (`web2-vuln-classes`, `bb-methodology`, `report-writing` → Strategist/Reporter prompts) were identified but deliberately left as flagged follow-up work rather than drafted from a one-line skill description. | Direct instruction to formalize the chat analysis as a document |
| 51 | **Standalone-engine comparison formalized** (`17-Standalone-Engine-Reuse-and-Comparison.md`). A deep-research pass on `claude-bug-bounty`'s `agent.py`/`brain.py`/`engine.py` (previously excluded from `16` for being non-Claude-Code, which turned out to be exactly backwards — they're architecturally *closer* to this system) surfaced a critical **safety issue first**: `hunt-memory/`, `findings/`, `recon/`, and `target_config.yaml` in that source repo contain real, live client/target data (including NASA subdomains and a live-looking Bearer JWT) and were explicitly never copied — confirmed twice, by the research pass and independently by the operator. Every file actually copied (`agent.py`/`brain.py`/`engine.py`, `requirements.txt`, `config.example.json`, `AGENTS.md`, `OPENCODE.md`, `install.sh`/`install_tools.sh`, `tests/`, `wordlists/`, and three `reports/*/gen_*.py` generator scripts) was individually verified clean of real data first, including a specific check that distinguished the safe `reports/{arsenal,poc_portfolio,portfolio}/gen_*.py` (operator's own tooling) from the unsafe `findings/*/reports/pdf/gen_*.py` (real per-client vulnerability content embedded as Python source, confirmed by name-frequency grep before assuming either way). Four gaps were found by direct comparison and resolved: **C-26** (no mechanical check that the Reporter's draft stays grounded in its own evidence → `FR-COUNCIL-17b`/`IR-GROUND-01..03`, mined from a verified-working `_ground_report_output()`), **C-27** (no failure-based circuit breaker, only a yield-based one → `FR-COUNCIL-11b`, threshold set to 3 to match the existing breaker rather than the source's own value of 5), **C-28** (no rate limiting anywhere → `FR-TOOL-14`/`IR-BRIDGE-05`, two-speed 10/s-default vs 1/s-high-risk-category, mapped onto categories we already have rather than a new taxonomy — the source's per-request human-approval tier was explicitly not adopted, since it conflicts with this system's confirmed fully-unattended design). **C-29** (context-window management over a long resident-Operator loop) was investigated but **left genuinely unresolved** — the source's own claimed technique couldn't be verified, and this document set's standard has been to mine verified techniques, not fabricate plausible ones — logged as Open Item H. | User pointed at the source repo directly and asked what should be fetched; research fork found the safety issue and the four comparison points; four rounds of direct questions confirmed each resolution (grounding: yes; failure breaker: yes, threshold 3; rate limiting: yes, two-speed) |
| 52 | **Full corpus audit** (requested explicitly, plus a follow-up request to check for development-blocking issues). A direct mechanical scan (numeric-value consistency: `-900`, `1.5GB`, `85%/95%`, `20s`, all confirmed clean) plus four parallel deep-audit passes found and fixed real, previously-undetected problems: **(1) schema-enum drift** — `FR-COUNCIL-17b` said `BLOCKED` where the schema says `BLOCKED_UNGROUNDED`, and `FR-COUNCIL-12`'s Operator-unload condition omitted the `UNREACHABLE` status added later by `FR-COUNCIL-11b`/C-27 (a real ripple-through gap, now fixed on both). **(2) Eight critical-analysis findings (C-01, C-02, C-04, C-05, C-07, C-08, C-09, C-10) were resolved elsewhere in the corpus but silently missing their own "Resolution" write-back paragraph in `11`** — added for all eight, plus the missing C-26–C-29 rows in `11`'s summary table, plus a rewrite of `11`'s closing paragraph, which had gone stale and was actively contradicting the rest of the document (claiming nothing had been resolved, when 28 of 29 findings had been). **(3) `00`'s backup lookup table and narrative section had not been updated past roughly C-14/decision #40** — missing rows for `16`/`17`, a stale "C-01 through C-14" description, and a two-"Update"-paragraph narrative that never acknowledged C-19 through C-29 or decisions #41-51 — rewritten as one accurate, consolidated summary. **(4) Six stale `01`-`15` (should be `01`-`17`) range references** across `00`, `CLAUDE.md`, `07`, `08`. **(5) `13`'s module-layout comments were stale relative to C-19 through C-29** — missing `DR-SCHEMA-13`/`14`, `FR-COUNCIL-11b`/`14a`/`17a`/`17b`, and had no designated module at all for the rate limiter, the network-failure classifier, or the Offline Script Linter — all five added. **(6) A genuinely ambiguous config key**, `grounding_retry_attempts: 3`, which reads naturally as "3 retries beyond the first attempt" (4 total) when the intended meaning is 3 attempts total — renamed to `grounding_max_attempts` with an explicit inline clarification. **(7)** `14`'s Reporter section header didn't mention `FR-COUNCIL-17b`, and `15` undersold its own traceability claim as `01`-`14` instead of `01`-`17` — both fixed. No milestone-ordering bug was found in `15` (checked and confirmed clean). | Explicit instruction to audit all requirements, plus a follow-up to check for development-blocking issues; four parallel fork audits plus a direct mechanical scan, all independently verified against live file content rather than assumed correct |
| 53 | **Open Item E resolved:** the test lab for `09`'s acceptance tests and `15`'s Milestone 8 full pass is confirmed as a local, disposable, Docker-based intentionally-vulnerable environment (e.g. OWASP Juice Shop, DVWA, a throwaway network-service container) — never real infrastructure. Exact composition is left as an implementation-time detail (`AC-ASSUME-06`). A traceability matrix (`18-Requirement-to-Test-Traceability-Matrix.md`) was also requested, cross-referencing every requirement ID against which `09` test (if any) covers it. | Direct answers to both questions in the same round |
| 54 | **MCP server verification follow-up, prompted by a direct question ("have Burp/Caido/Playwright's MCP configs been fetched?").** Re-checked what `16` had left unverified: `mcp/caido-mcp-client/` is confirmed **genuinely real MCP** (an `npx @caido/mcp-server` registration), unlike Burp's REST-bridge shortcut — this revises `16`'s original conclusion that "no MCP client is ever needed" to "not needed for Burp/HackerOne, but would be for Caido if that integration is ever pursued." No Playwright MCP configuration exists anywhere in the source repo at all (no `mcpServers` key in `.claude/settings.json`, no dedicated folder analogous to Burp/Caido) — it's registered outside the repo entirely, most likely a global/user-level Claude Code registration, so there was never anything to fetch. This check also surfaced a new sensitive-data category not caught in the original safety pass: `.claude/settings.local.json` contains a permission allowlist of real attack commands against a real client (`myco.io`) — confirmed never copied, added to `17`'s safety notice alongside `.playwright-mcp/`'s runtime session cache (also never copied). | Direct question prompted re-verification of a previously-flagged "unverified" item rather than leaving it unchecked |
| 55 | **6-model council roster overhaul, prompted by a Hugging Face gating check.** Operator flagged not holding a license for gated models; `Llama-3.1-8B-Instruct` (Gate 1's semantic tier since decision #34) turned out to be exactly that. Rather than a narrow substitution, the operator supplied a complete revised roster, resolved across several rounds of pushback: **Strategist** becomes `DeepSeek-R1-0528-Qwen3-8B` (a different, newer DeepSeek release than `DeepSeek-R1-Distill-Qwen-8B`); **Gate 1 (semantic tier)** reverts to `Hermes-3-Llama-3.1-8B` — reopening part of C-03's original concern, resolved by re-framing Tier 0's deterministic pre-check (not Tier 1) as the actual non-bypassable safety boundary, with Tier 1 downgraded to a contextual/strategic sanity check only (see C-03's "Update" in `11`); **Gate 2's offline linter** stays `Qwen2.5-Coder-3B-Instruct`, explicitly confirmed unchanged from a fully-deterministic active-loop design (no reopening of C-09); **Reporter** is split out as a genuinely separate 6th resident model, `Ministral-8B-Instruct-2410`, rather than reloading the Strategist's own weights; every model moves to uniform `Q8_0` quantization. The `Q8_0` switch was flagged as tightening RAM headroom significantly (worst case ~5.2 GB → ~2.4 GB) — accepted as C-30, a documented trade-off, since it still clears the confirmed 1.5 GB safety margin. Applied everywhere: `Agentic VAPT Setup (HOME).md`, `01`-`03`, `05`, `07`-`11`, `13`-`15`, `17`. | Operator flagged the Llama gating problem directly; three rounds of pushback questions (Gate 1 refusal-capability trade-off, Gate 2 role-description ambiguity, DeepSeek model identity/6th-model confirmation) before implementing, consistent with challenging an answer that reopens a previously-resolved finding rather than applying it silently |
| 56 | **"Fetch everything useful" mining sweep of `/home/vscysteam/claude-bug-bounty`**, run as two parallel background investigations (a diff-focused pass, a full-tree fresh-mining pass) plus direct verification of anything either one recommended before acting on it. Findings: (1) a real, safe, mechanical upstream rename (`findings/`→`recon/` output-path convention) synced across 4 command docs + 4 tool scripts in `Actual-Setup/`. (2) Two newly-created files in the source's `rules/` — `NASA_VDP_Intel.md`, `VRT_Reference.md` — contain real NASA VDP program data; confirmed excluded, names only, no content quoted, consistent with the standing safety notice. (3) A fork's claim that `memory/audit_log.py` was "never copied" was independently re-checked and found **wrong** — the entire `memory/` package already exists byte-identical under `Actual-Setup/memory/` (copied earlier for an unrelated reason); `17`'s misattribution of `CircuitBreaker`/`RateLimiter`/`AutopilotGuard` to `agent.py` itself (rather than to `memory/audit_log.py`, which `agent.py` imports) was corrected instead. (4) Three previously-deferred skill files (decision #50, `16` §4b) were now fully read and mined: `bb-methodology`'s assumption-breaking checklist → Strategist prompt (`14` §1); `report-writing`'s title formula and "never write 'could potentially'" rule → Reporter prompt (`14` §5); `security-arsenal` (1,668 lines) was read but judged not transferable into any prompt (the Operator's own coding-model training already covers this payload knowledge; embedding it would bloat `FR-GATE-07`'s context ceiling for marginal benefit). (5) `web2-vuln-classes` §11 ("LLM/AI Features → MCP & RAG-Specific Attacks") surfaced a genuine, previously-undocumented gap — **new finding C-31**: `FR-TOOL-13`'s heuristic injection detector had no named patterns for Unicode Tag-block ASCII-smuggling, MCP tool-description "line jumping," or split/obfuscated-instruction evasion, all directly relevant since this system is itself an LLM agent processing untrusted tool/MCP output. Resolved by adding these as named patterns to `FR-TOOL-13`, remaining detection-only/SHOULD-level — the actual safety boundary (`IR-SANITIZE-02/03`) is unchanged. (6) `docs/TODOS.md` (new, copied into `Standalone-Engine-Reference/`) confirmed its `SafeMethodPolicy` (HTTP-method-level approval gating) is the *same* mechanism `17`'s C-28 resolution already evaluated and declined, under a different name — not a new gap, no new requirement follows. (7) Several specialized skills present in the source (`web3-audit`, `mobile-pentest`, `meme-coin-audit`, `graphql-audit`, `cicd-security`, `credential-attack`, etc.) were noted but not mined — deliberately left to an explicit operator scope decision rather than assumed in-scope, since adopting them would expand this system's stated purpose beyond general web/network VAPT. | Operator instruction to "find everything useful... fetch everything," with an explicit prior instruction that ambiguous "flags" language meant this project's own open items, not claude-bug-bounty's internal ones — investigated via evidence, not assumed resolved by assertion; independent re-verification caught one fork claim (memory/audit_log.py "never copied") that was actually wrong |
| 57 | **Full scope expansion: every remaining skill from decision #56 brought into explicit scope**, via 8 parallel deep-mining forks covering all 14 remaining/partial skills (`web3-audit`, `meme-coin-audit`, `mobile-pentest`, `graphql-audit`, `cicd-security`, `credential-attack`, `diff-review`, `whitebox-code-recon`, `capability-chaining`, `client-reverse`, `argus`, `web2-recon`, `bug-bounty`, the remaining 31 classes of `web2-vuln-classes`), plus a direct (non-delegated) read of the sensitive `opt-in-advanced-techniques` file given its anti-forensics content. New document `19-Extended-Capability-Domains.md` formalizes 9 genuinely new capability domains (`FR-VULNCLASS`/`FR-WEB3`/`FR-MOBILE`/`FR-GRAPHQL`/`FR-CICD`/`FR-CRED`/`FR-CODEACCESS`/`FR-ARGUS`/anti-forensics+broad-scope-framing); pure reasoning/technique content (`capability-chaining`, `client-reverse`, `bug-bounty`'s chain method and hacker mindset, `web2-recon`'s stack-routing table) was folded directly into the Strategist/Operator prompts (`14`) instead, on the same "domain vs. technique" distinction the forks themselves drew. Three structural tensions were raised for explicit operator decision rather than silently resolved: **(a)** four techniques (`opt-in-advanced-techniques`'s anti-forensics, `credential-attack`'s live `/spray`, `cicd-security`'s real-PR-opening actions, `web2-vuln-classes`'s dependency-confusion live-publish) each have a *source-material* safety mechanism that is a live human confirming something in real time — incompatible with this system's fully-autonomous, no-pause design as previously specified. Resolved: **preserve the capability, add a narrow exception** — a new Human Checkpoint Gate (`FR-CHECKPOINT-01..05`, `DR-SCHEMA-18`) that hard-stops only for these four named action classes, requiring a live `approve-checkpoint`/`deny-checkpoint` CLI action with no auto-timeout, layered on top of (not instead of) the existing opt-in-flag system; full rationale for each of the four catalogued separately in new doc `20-Human-Checkpoint-and-Escalation-Safety-Catalog.md`, per explicit operator instruction not to bury this reasoning inside `19`'s individual domain sections. **(b)** Several domains need target-identity shapes the existing host/domain schema can't express (contract+chain-id, mobile-binary+platform, repo+ref) — resolved via full schema design now (`DR-SCHEMA-15/16` in `03`: a `target_type` discriminator + type-specific nullable columns on `targets`, and a `pattern_kind` discriminator on `scope_rules` for `EXACT_IDENTIFIER`/`PATH_GLOB` matching), not deferred. **(c)** `meme-coin-audit`'s own stated purpose ("pre-investment due diligence") has no client/scope_rules relationship at all, unlike every other domain — resolved: **keep both modes** (`contract_investigation_mode`: `CLIENT_OWNED` / `PUBLIC_RESEARCH`), with `PUBLIC_RESEARCH` restricted to read-only public-data queries only, no intrusive testing. **(d)** `web2-recon`'s continuous-monitoring pattern conflicts with the per-invocation, no-daemon process model — resolved: **add scheduling without a daemon** (`FR-MONITOR-01..04`: an external cron/systemd-timer-triggered `vaptctl monitor` invocation, deterministic and model-free, discovery-only, never auto-escalating into active testing, and deliberately exempt from the single-engagement lock since it never hibernates). Ripple updates applied to `01` (new `FR-CHECKPOINT`/`FR-MONITOR` sections), `03` (`DR-SCHEMA-15..18`), `08` (`AC-DEPENDENCY-11..17`, new tooling: Foundry+RPC, adb/apktool/jadx/frida/objection, GraphQL tool set, sisakulint/gh, cewler/hashcat, interactsh-client, a CDP-capable headless browser), `09` (`TP-CHECKPOINT`, `TP-MONITOR`), `13` (new CLI commands and modules), `14` (Strategist/Operator prompt additions). | Four-question consolidated round on the structural tensions before writing `19`: preserve-capability-plus-checkpoint (not exclude, not silently weaken) for (a); full schema design now, not deferred, for (b); keep both modes for (c); add scheduling for (d) — each answer directly shaped the architecture built afterward rather than being retrofitted |
| 58 | **Final "mine everything" pass over `/home/vscysteam/claude-bug-bounty`'s remaining ~250KB of unread content** (`docs/`, `web3/`'s 14 deeper reference files, `scripts/`, `hooks/`, `site/`) via 4 parallel forks, prompted by the operator directly asking "why it was left" rather than accepting the earlier scope-decision framing at face value — a fair challenge, since these files simply hadn't been read yet, not deliberately excluded. Two filenames (`web3/07-case-study-role-misconfiguration.md`, `09-live-hunt-zksync.md`) were flagged for extra safety scrutiny given their "case study"/"live hunt" naming; verified safe via direct read plus grep (07 is explicitly self-labeled "Anonymized" with no real names found; 09 documents a zero-finding audit of ZKsync Era, a public open-source protocol under a public Immunefi program) before mining, then independently re-verified a second time (grep for known real hostnames, a private-key/seed-phrase pattern check) before copying anything. Concrete additions: `FR-TOOL-15` (auth-session-credential-plumbing, `01`); `FR-VULNCLASS-04/05` (timing side-channels, XS-Leaks, MiniKit/WebView spoofing, framework-specific playbooks, `19`); `FR-WEB3-07..11` (DEX/LP attacks, Solana Token-2022 Permanent-Delegate/Transfer-Hook risks, Slither/Echidna/Medusa as a distinct static/fuzz testing layer, a genuine new `solidity-audit-mcp` MCP integration, `19`); `AC-DEPENDENCY-18/19` (`08`); a concrete pipeline-sequencing addition to the Operator prompt (`14` §3) mined from `full_hunt.sh`'s actual gf-filter-then-target-confirmation-tool chaining, per the operator's explicit emphasis that shell-script/pipeline logic (not just methodology prose) is the highest-value category. `scripts/full_hunt.sh`, `dork_runner.py`, all 15 `web3/*.md` files, `docs/{advanced-techniques,payloads,auth-sessions,TUTORIAL,smart-contract-audit}.md`, `demo/app.py`, and `hooks/hooks.json` were all copied into the project (verified byte-identical and independently re-checked clean) — `site/index.html` was assessed and deliberately not copied (a marketing page with no unique technical content). Also produced new document `21-Safety-Ethics-and-Misuse-Prevention-Control-Inventory.md`, a complete catalog (location/purpose/impact) of every misuse-prevention control across `01`-`20`, per an explicit operator request distinct from the mining sweep itself. | Operator: "Yes... Do It... Why it was left... Mine out Each and Everything," plus a separate request to document every safety/ethical-use control in complete detail with file location, purpose, and impact |
| 59 | **True final completeness check before pushing**, prompted directly by the operator asking to confirm nothing useful remained. A `find -maxdepth 1` listing of the entire source repo caught genuine top-level gaps every prior pass had missed: `README.md`, `CHANGELOG.md`, `FAQ.md`, a top-level `SKILL.md`, `LICENSE`, `TERMS.md`, `serve.py`, `pytest.ini`, `uninstall.sh`/`uninstall_tools.sh`, and `.github/`. All read/checked directly or via one final fork. Findings: top-level `SKILL.md` is a **stale, shorter duplicate** of the already-mined `Actual-Setup/skills/bug-bounty/SKILL.md` — confirmed via direct diff, no action needed, the already-copied file is authoritative. `TERMS.md` (MIT-licensed project's own ethical-use terms) and `FAQ.md`'s "Safety & Legal" section independently corroborate this project's own safety design in the source's own voice — cited into `21`'s new §J rather than treated as new requirements. `CHANGELOG.md` surfaced one genuine gap: a "Category 6: AI Agent Security" CI/CD sub-class (unrestricted AI-agent triggers, excessive tool grants, prompt injection via workflow context) not in `FR-CICD-01` — added. The top-level `SKILL.md`'s "LEAD BOARD" signal-routing concept surfaced a second genuine gap — explicit cross-domain follow-on-task routing wasn't specified now that `19` covers 9+ domains — added as `FR-COUNCIL-10a`. `LICENSE` (MIT) was retained per its own attribution requirement given how much of this repo has been copied. `.github/` (issue templates, `SECURITY.md`, etc.) and `site/index.html` were confirmed as the source repo's own project-governance/marketing material, not reference content — deliberately left uncopied, not overlooked. | Operator: "Anything that you want to ask me!? If None, then push to Github after Confirming that nothing Useful or Potentially Useful has been left... to be mined and copied" — the confirmation was executed as an actual fresh completeness check (a full top-level directory listing cross-referenced against everything already covered), not asserted from memory |
| 60 | **VAPT Monitoring Dashboard formalized** (`22-VAPT-Monitoring-Dashboard-Specification.md`), from an operator-supplied, near-implementation-ready draft spec (`vaptctl dashboard`, a live `rich`/`plotext` terminal UI with a turn/time-forecasting engine). A consistency-check pass against the actual current schema/requirements (not assumed correct from the spec's own confidence) found four real issues, each raised as an explicit question rather than silently fixed or silently accepted: **(1)** the spec's `sqlite3.connect(..."mode=ro&immutable=1"...)` is objectively incorrect for observing a live, actively-written WAL-mode database — `immutable=1` asserts the file never changes, which is false here — resolved to plain `mode=ro`. **(2)** the spec introduced a brand-new, previously-unconfirmed numeric threshold ("swap >2 GiB = red") with no corresponding value in `NFR-RES-06` — resolved by formally adopting 2 GiB as that threshold's confirmed value (`02`, revised), closing a gap `09`'s own test-plan note had flagged as unconfirmed. **(3)** the spec's command name (`vapt dashboard`) didn't match the confirmed `vaptctl` CLI (`13`) — resolved as `vaptctl dashboard`, no CLI rename. **(4)** the spec's SQL referenced a non-existent `model_invocations` table/columns and a non-existent `engine_state` table — resolved by deriving everything from the real `model_invocation_logs` table (`03`, revised to add a `turn_number` column and give `ended_at IS NULL` load-bearing "currently running" meaning), avoiding a second state store that would need to be kept in sync with the real one; the operator's own follow-up answer supplied a complete, more precise formula set (EMA-of-last-3 rather than a flat mean-of-5, a 0.10 retry-ratio floor, explicit cold-start priors) beyond what was strictly asked. A fifth, non-schema issue was also raised and fixed: the original mockup conflated the fully-deterministic Gate 2 (`FR-COUNCIL-08`, no model) with the separate, rarely-invoked Offline Script Linter (`Qwen2.5-Coder-3B`, `FR-COUNCIL-09a`) into one row, and showed two roles simultaneously non-`COLD` in violation of `FR-GATE-02`'s single-residency policy — both corrected: Gate 2 and the Offline Linter are now separate rows, `RESIDENT`/`IDLE` is restricted to the Operator role only (every other role tears down immediately per `FR-GATE-05`), and a live single-residency violation now renders as an explicit integrity alert rather than a silently-accepted display state. Also surfaced, and fixed while auditing this in passing: `model_invocation_logs.role`'s enum was stale, missing `Reporter` entirely since decision #55 split it into its own model — added. Ripple updates: `01` (`FR-DASHBOARD-01..12`), `02` (`NFR-RES-06`), `03` (`DR-SCHEMA-08`), `08` (`AC-DEPENDENCY-20`: `rich`/`plotext`/`psutil`), `09` (`TP-DASHBOARD`), `13` (CLI + module layout). | Operator supplied the full draft spec and explicitly asked me to "ask everything... as developer of this feature," check consistency, and wait for answers before proceeding — a 4-question consolidated round surfaced every real inconsistency found; the operator's answers went beyond yes/no, supplying a fully worked formula/write-path blueprint for question 3 |
| 61 | **`Agentic VAPT Setup (HOME).md` full retroactive mirroring pass** — a direct question ("is HOME.md continuously updated?") revealed it wasn't: decision #55's model roster was mirrored, but every capability/mechanism added since (decisions #56-60: the full extended-domain expansion, the Human Checkpoint Gate, the monitoring dashboard) had zero mentions anywhere in the base file. Also confirmed, in the same investigation: a separately-raised concern about "2 of 6 models repeated" was checked directly (every model-identifier occurrence counted across the whole file) and found to be a false alarm — all 6 are genuinely unique everywhere; likely the `Qwen2.5-Coder-7B-Instruct`/`Qwen2.5-Coder-3B-Instruct` family-name similarity read as a duplicate on a skim. Confirmed principle, stated directly: **`HOME.md` should act as a complete high-level blueprint of the whole system**, not just the original 5-phase autonomous loop — narrower than decision #42's original framing ("mirror corrections to an existing claim, not purely-additive new mechanisms"), which had already left several older additions unmirrored too (the entire `FR-CTRL` operator control surface and kill-switch, prompt-injection defense, structured-output enforcement, report grounding, rate limiting, the failure-based circuit breaker, and multi-target support). Resolved as a **full retroactive pass**, not just the newest gap: four new high-level sections added (§5 Operator Control Surface & Emergency Stop, §6 Defense-in-Depth Safeguards, §7 Extended Capability Domains & the Human Checkpoint Gate, §8 Live Monitoring & Scheduled Checks), plus a one-line multi-target clarification folded into the existing Phase 1 database-initialization bullet — all at the same high-level altitude as the rest of the file (decision #42's altitude split itself is unchanged; only its "corrections only" scope narrowed). | Operator: "Is `Agentic VAPT Setup (HOME).md` continuously updated with the Major Changes... Which 06 Models are we using!? ... lists 06 but have 02 repeated" followed by "Should act as a High Level BluePrint for Whole Setup!!" and, when asked how far back to go, "Full retroactive pass (recommended)" |
| 62 | **Interactive TUI Console & Intervention Pipeline formalized** (`23-Interactive-TUI-Console-and-Intervention-Pipeline-Specification.md`), from an operator-supplied, near-implementation-ready draft (`vaptctl console`, a live `Textual`-based activity stream + append-only Markdown journal + role-directed mid-engagement operator directives). A consistency-check round found four issues, two of them serious enough to push back on rather than implement as first stated: **(1)** the role-prefix list and `DR-SCHEMA-19`'s CHECK constraint both omitted the Offline Script Linter (the system's 6th model) — resolved by adding an `@lint` prefix, a `Linter` CHECK-constraint value, and its own (rare, between-phase) `FR-INTERVENE-07` invocation point. **(2)** `FR-INTERVENE-06`'s "operator instruction MUST take precedence" was first answered as "operator directives can override safety gates" — pushed back on directly, since this would have meant a console command could bypass `FR-COUNCIL-06`'s non-bypassable scope check, the Tier 2 denylist, opt-in-flag validation, and rate limiting, turning any console compromise into a full safety-architecture bypass point. The operator reconsidered and confirmed the narrower, coherent model actually intended: operator precedence is **reasoning-weight only** (which hypothesis/target to pursue, how heavily to weigh a conflicting instruction) — every deterministic gate evaluates a console-influenced command exactly as it would an autonomous one, with zero exception. **(3)** whether an operator explicitly dispatching a Human-Checkpoint-class action via console still needs the formal `approve-checkpoint` pause — resolved: an explicit, specific console dispatch **is** the live human attestation the checkpoint gate exists to obtain, auto-finalizing the `checkpoint_events` row (`approved_via = 'CONSOLE_DISPATCH'`) and skipping only the pause — never `FR-CHECKPOINT-02`'s pre-engagement flag gate, `FR-CHECKPOINT-05`'s anti-forensics attestation fields, or Tier 0/Tier 2/rate-limiting; a *vague* directive the model later escalates into a checkpoint-class action on its own still gets the full pause. **(4)** `operator_command_queue`'s `EXPIRED` status had no defined trigger and no anti-silent-failure requirement — resolved with a new `failure_reason` column, mandatory on every `EXPIRED`/`DISCARDED` transition, with expiration triggered by a directive's target-role invocation window closing for that target, not a fixed TTL. A fifth, separately-raised concern — that redaction should be removed everywhere per `Actual-Setup/CLAUDE.md`'s "never redact evidence in a report" rule — was checked against the source text directly and found to be based on a misreading: that rule is scoped to the report specifically, and this system's existing `FR-COUNCIL-18` (draft-time redaction, restored byte-exact on `approve-report`, final report never redacted) already satisfies it to the letter; the operator confirmed the existing design stays unchanged once the actual scope was clarified. Ripple updates: `01` (`FR-CHECKPOINT-03` cross-reference), `03` (`DR-SCHEMA-19/20`, `DR-SCHEMA-18` amendment), `08` (`AC-DEPENDENCY-21`: `Textual`), `09` (`TP-TUI`), `13` (CLI + module layout + a note that all six existing council modules need a code change, not just new `23`-introduced code). | Operator: "New Requirement, Analyze & Check Feasibility and Ask me if there are any Issues" — a 4-question round surfaced all four issues; two answers (Q1, Q3) were pushed back on with specific, concrete risk/evidence before implementing, consistent with this session's standing "challenge unrealistic or unsafe answers" authority; the operator reconsidered both and supplied materially safer, more precise refined answers rather than insisting on the originals |
| 63 | **Gate 1 semantic-tier bypass for manual-operator-origin tasks** (amends `23`, decision #62). A follow-up request asked to formalize that "council guardrails exist solely to govern autonomous LLM behavior, not to restrict the authorized tester" by having a new `origin` tag (`AUTONOMOUS_COUNCIL`/`MANUAL_OPERATOR`) skip Gate 1 and the Checkpoint Gate entirely for operator-dispatched tasks, with "root authorization" / "do NOT question and execute immediately" language — the same bypass already walked back from in decision #62's item (2), now asked for again in a stronger form. Pushed back on with the same concrete-risk framing as #62: `FR-COUNCIL-03a`'s Tier-0 scope check exists to catch the operator's *own* mistakes (typos, scope drift) against the operator's *own* prior `scope_rules` configuration, not to gate the operator's authority — removing it for manual-origin tasks would make a compromised or misused console a single point of failure for the entire safety architecture. Offered three bounded alternatives (no bypass at all beyond audit tagging; a narrow Gate-1-semantic-tier-only bypass; the full bypass as asked, risk restated) and asked the operator to choose explicitly rather than silently implementing either extreme. **Resolved**: the narrow option, with the operator's own precise boundary refinement layered on top — (1) `origin` added to `task_queue` for telemetry/dashboard/audit only; (2) a `MANUAL_OPERATOR`-origin task skips only Gate 1's semantic tier (`Hermes-3`, `FR-COUNCIL-04`) — an explicit operator command already supplies the contextual/technical judgment that tier exists to make; (3) `FR-COUNCIL-03a` (Tier 0), `FR-TOOL-06`/`06a` (Tier 2 denylist, opt-in-flag validation), and `FR-TOOL-14` (rate limiting) remain fully enforced regardless of origin, with zero exception; (4) the Checkpoint Gate gets no new bypass beyond what `FR-INTERVENE-10` (decision #62) already specified — explicit dispatch is still only an attestation-pause skip, nothing more. `origin='MANUAL_OPERATOR'` is set only when a new `source_command_id` FK traces the task's `proposed_command` to an operator directive whose own text explicitly and specifically named the action (same specificity bar as `FR-INTERVENE-10`) — a vague directive the model expands into a command on its own keeps `origin='AUTONOMOUS_COUNCIL'` and the full two-tier gate. Ripple updates: `01` (`FR-COUNCIL-04`/`05`/`06` revised, `FR-COUNCIL-03a` clarified), `03` (`DR-SCHEMA-05` — `origin`/`source_command_id` columns), `09` (`TP-TUI` new test row), `13` (module-layout comment), `23` (new amendment section, `FR-INTERVENE-06a`, `DR-SCHEMA-05` amendment note, `TP-TUI` test row). | Operator asked for `origin`-tagged tasks where `MANUAL_OPERATOR` bypasses Gate 1 and the Checkpoint Gate "entirely," framed as guardrails not applying to the operator at all — pushed back on given the direct contradiction with #62's just-confirmed "zero exception" model; offered a 3-way choice (no bypass / narrow Gate-1-semantic-only bypass / full bypass as asked, risk restated); operator chose the narrow option and supplied a fully worked, more precise boundary definition (four numbered points) beyond a plain accept |
| 64 | **Historical State Inheritance & Deduplication Subsystem formalized** (`24-Historical-State-Inheritance-and-Deduplication-Specification.md`), from an operator-supplied, near-implementation-ready draft addressing three cross-engagement duplication failure modes (Strategist re-proposing exhausted hypotheses, Operator re-running identical scans, Adjudicator/Reporter re-reporting known vulnerabilities). A consistency-check round found five technical issues, resolved directly, plus one client-safety issue raised as an explicit question: **(1)** the draft's historical-path query filtered on `attack_paths.status IN (..., 'COMPLETED', 'EXHAUSTED')`, values that don't exist in `DR-SCHEMA-04` and have no defined transition trigger anywhere — resolved by deriving "explored" from existing `task_queue` execution status instead of inventing an unreachable enum, the same principle decision #60 used to avoid a second, unsynced state store. **(2)** the draft's Gate 2 dedup invented a new "skip to next task" rejection pathway alongside the one `FR-COUNCIL-09` already defines — resolved by routing `DUPLICATE_COMMAND` through the existing regenerate/`BLOCKED` pipeline as just another rejection reason (which also delivers "no execution budget consumed" for free, since `task_count` only increments on actual execution). **(3)** the draft's `command_hash` canonicalized by sorting the flat argv token list, which breaks flag/value pairing and would hash `-p 80 -oN 443` and `-p 443 -oN 80` identically — a false-positive dedup bug, not a style nit — resolved with pair-aware canonicalization (group flag+value before sorting). **(4)** the draft's fingerprint index/query was keyed on `target_id`, which is engagement-scoped (`DR-SCHEMA-02`) and therefore structurally can't find a prior engagement's rows — resolved by joining through `targets.host_or_domain`, mirroring the pattern the draft's own `attack_paths` query already used correctly. **(5)** (side finding, unrelated) `13`'s CLI already carried a dangling `--mode assess|monitor` flag with no backing requirement anywhere — replaced by this feature's real `--assessment-mode` flag rather than left alongside it. **(6)** the client-safety issue: the draft's `FR-DEDUP-03` had Gate 3 auto-exclude any `CONFIRMED`-fingerprint match entirely from the new report — since this system has no regression-verification concept, a client whose vulnerability was never actually fixed would simply never see it mentioned in a fresh assessment report. Raised directly rather than implemented as specified, with three options offered (never omit / full omission as specified / operator-gated at report-approval time). **Resolved, substantially expanded** by the operator's own follow-up design: an explicit **Assessment Mode** dimension (`engagements.assessment_mode`, `INITIAL`/`RETEST`) replaces passive fingerprint-exclusion with active regression verification — in `RETEST` mode, prior `CONFIRMED` findings are seeded as priority `task_queue` rows (new `origin = 'HISTORICAL_REGRESSION'`, extending decision #63's `origin` column to a third value that gets **zero** gate exceptions, unlike `MANUAL_OPERATOR`) and re-verified through the full two-tier Gate 1 and Gate 2 before the Strategist's fresh exploration begins for that target; the outcome is always visible — still-vulnerable findings get their own `VAPT_FINDING` report explicitly marked carried-forward, remediated ones (`status = 'REMEDIATED'`, new 4th value) are surfaced in the `INFO_REGISTER` as a verified fix — never a silent drop from either document. Ripple updates: `01` (`FR-COUNCIL-15`/`17` extended for report routing), `03` (`DR-SCHEMA-01` `assessment_mode`; `DR-SCHEMA-05` `origin` extended + `source_finding_id`; `DR-SCHEMA-06` `command_hash`; `DR-SCHEMA-07` `finding_fingerprint`/`target_endpoint`/`affected_parameter`/`finding_origin`/`retests_finding_id`/`REMEDIATED` status), `09` (`TP-DEDUP`), `13` (CLI `--assessment-mode` replacing the dangling `--mode` flag, module layout), `00`/`CLAUDE.md` (index entries), `24` (new). | Operator supplied the full draft spec; a 6-issue consistency-check round found five objective technical bugs (resolved directly, same rigor as #60's `mode=ro` fix) and surfaced one real client-safety risk as an explicit 3-way question rather than silently implementing the draft's "auto-exclude" behavior; the operator's answer went well beyond picking an option, supplying a fully worked dual-mode (`INITIAL`/`RETEST`) architecture with its own CLI flag, schema, and phase-sequencing design |

---

## Open Questions Remaining

These are the only items in the requirement set that are **not yet closed** by an
explicit operator decision — everything else in `01`-`17` traces to the log above.

| # | Open Item | Why It's Still Open |
|---|---|---|
| A | Real-hardware feasibility checks (`TP-FEASIBILITY` in `09`): thermal/throttle telemetry availability, actual `i915`/`xe` driver binding, SYCL backend stability under sustained load | These can only be verified on the actual target machine at deployment time — nothing in a planning phase can close them |
| B | Cross-machine transfer (`RISK-CROSSMACHINE` in `07`) | This entire document set, plus `12-Report-Formatting-Rules.md` and the `claude-bug-bounty` toolkit it was cloned from, currently exist only on this planning session's machine, not on the target PC described in the base document |
| C | `RISK-UNCENSOREDGATE`-adjacent residual, revised by decision #55: Council Gate 1's semantic layer is `Hermes-3-Llama-3.1-8B` again (reverted from `Llama-3.1-8B-Instruct`, which required a license the operator doesn't hold). The reframing — Tier 0's deterministic pre-check is the real safety boundary, Tier 1 is contextual-only — is coherent, but no test has verified Tier 1 actually catches the "technically in-scope but excessive" class of task it's now solely responsible for; a reduced-refusal model could plausibly under-flag exactly that class | Requires the actual model file and a live evaluation, out of reach of a planning-phase document |
| D | Whether the three high-risk category binary lists (decision #26) are exhaustive, or whether other Kali tools should be added to `--allow-brute-force`/`--allow-active-exploitation`/`--allow-lateral-movement` as they're discovered in practice | The lists as confirmed are believed complete for common cases but were not cross-checked against the full `kali-linux-everything` package manifest |
| F | Model `.gguf` file acquisition (source/quantization provenance for the 6-model roster confirmed in decision #55: `DeepSeek-R1-0528-Qwen3-8B`, `Qwen2.5-Coder-7B-Instruct`, `Hermes-3-Llama-3.1-8B`, `Qwen2.5-Coder-3B-Instruct`, `Mistral-7B-Instruct-v0.3`, `Ministral-8B-Instruct-2410`, all `Q8_0`) is nobody's job yet. The roster was specifically revised to avoid any gated/license-click-through model, so this item is no longer blocked on licensing — only on the mechanical acquisition step itself | Out of scope by this planning phase's own constraint (no downloads/installation), but a real prerequisite before Milestone 3 can start |
| G | No pinned dependency manifest (`requirements.txt`/`pyproject.toml`) for the confirmed Python stack (Click, PyYAML, the `cvss` library, an OpenAI-compatible HTTP client) | Raised during the "if I had to build this" review; deferred as an installation-adjacent detail rather than a planning-document question |
| H | Context-window management strategy for the Operator across a long (up to 30-task) per-target loop against its 16k ceiling (`FR-GATE-07`) | Critical-analysis finding C-29 — a comparable system (`claude-bug-bounty`'s `agent.py`) claims a "compress every 5 steps" technique, but the actual mechanism could not be verified in the research pass that found it, and fabricating one would violate this document set's evidence standard. Genuinely open. |

No other assumptions were made in place of a decision. If a future reader finds a
requirement in `01`-`09` that looks like an unstated assumption, it should be treated
as a documentation gap to raise, not as settled — cross-check against this log first.
